# Maestro Organisatie B Platform - Screenshots Required

## Manual Testing Required

The implementation is complete but requires manual browser testing with authentication to capture screenshots. The following pages need to be verified:

### 1. Navigation Check
**URL**: `/organisatie/health` (or any Organisatie page)

**Expected behavior**:
- ✅ Espresso sidebar (main Dashboard nav) stays visible on the left
- ✅ Organisatie nav pane appears in the middle (Settings-style layout)
- ✅ Content area on the right shows selected page
- ✅ NO "← Dashboard" button (that was AdminShell, now removed)
- ✅ Layout matches Instellingen (/instellingen) pattern

### 2. Organisatie Navigation Structure
Expected nav groups in Organisatie pane:

**Toegang**
- Gebruikers (with count badge)

**Bewaking**
- Health (with attention badge if > 0)
- Security (with findings badge if > 0)

**Kennis**
- Skills
- Legal AI

**Intelligence**
- Pijplijn
- Kwaliteit

**Infrastructuur**
- Platform (new merged page)
- API Keys

**Systeem**
- Wat is nieuw

### 3. Platform Page (B Layout)
**URL**: `/organisatie/platform`

**Expected layout**:
- Cockpit at top with 3 KPI cards:
  - Edge Functions (ok/error counts)
  - Platform Status
  - Last refreshed timestamp
- Segment switcher: `Configuratie | Edge Functions | Database Sync`
- Content area shows active segment

### 4. Screenshots Needed

Create these screenshots in `docs/previews/v1.168/`:

1. **organisatie-nav-espresso-sidebar.png**
   - Shows Espresso sidebar + Organisatie nav pane + Health content
   - Proves side-by-side layout (not AdminShell fullscreen)

2. **platform-page-configuratie.png**
   - Platform page with Configuratie segment active
   - Shows cockpit KPIs + config items grid

3. **platform-page-edge-functions.png**
   - Platform page with Edge Functions segment active
   - Shows function health cards

4. **platform-page-database-sync.png**
   - Platform page with Database Sync segment active
   - Shows Truth of Sources cards

### 5. Redirects to Test

All these old paths should redirect properly:
- `/admin/*` → `/organisatie/*`
- `/admin/configuratie` → `/organisatie/platform`
- `/admin/edge-functions` → `/organisatie/platform`
- `/admin/database` → `/organisatie/platform`
- `/admin/deployments` → `/organisatie/health`

## Why Manual Testing Required

- Requires authenticated Supabase session
- Supabase credentials not available in cloud agent environment
- Real data needed for badges/counts display
- Visual verification of Espresso + Organisatie side-by-side layout

## Build Status

✅ `npm run build` passes
✅ All imports resolved
✅ TypeScript types valid (JS codebase)
✅ No console errors

## Code Verification

✅ OrganisatieView uses SettingsLayout (same as Instellingen)
✅ Dashboard routes Organisatie within main shell
✅ App.jsx no longer switches to AdminShell
✅ All admin pages accessible via /organisatie/* paths
✅ Version bumped to v1.168 (avoiding conflict with PR #82)
