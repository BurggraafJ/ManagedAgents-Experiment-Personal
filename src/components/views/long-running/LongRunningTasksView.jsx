import InConstruction from '../../ui/InConstruction'

/**
 * LongRunningTasksView (v1.127) — desktop-stub voor de module "Long running
 * tasks": taken die langer lopen dan één agent-run en over dagen gevolgd
 * worden. Eigen top-level view (Operations-groep), bewust níét onder
 * Instellingen. Titel + intro komen uit VIEWS in App.jsx (view__header).
 */
export default function LongRunningTasksView() {
  return (
    <InConstruction what="Long running tasks is in opbouw. Hier komt straks het overzicht van taken die meerdere runs of dagen lopen, met voortgang en laatste stap." />
  )
}
